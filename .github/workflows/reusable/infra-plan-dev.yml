name: infra-plan-dev

on:
  pull_request:

env:
  TF_CLOUD_ORGANIZATION: replace-with-tfc-organization
  TF_API_TOKEN: ${{ secrets.TF_API_TOKEN_DEV_PLAN }}
  TF_WORKSPACE: ${{ github.event.repository.name }}-dev
  CONFIG_DIRECTORY: ./infra

permissions:
  contents: read
  pull-requests: write

jobs:
  terraform-plan-dev:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Terrascan IaC scan
        shell: bash
        run: |
          set -euo pipefail
          docker run --rm -v "${PWD}:/iac" tenable/terrascan scan -t terraform -i terraform -d /iac/infra

      - name: Prepare dev tfvars for remote run
        shell: bash
        run: cp infra/env/dev.tfvars infra/terraform.tfvars

      - name: Upload Terraform configuration (speculative)
        id: plan-upload
        uses: hashicorp/tfc-workflows-github/actions/upload-configuration@v1.3.2
        with:
          workspace: ${{ env.TF_WORKSPACE }}
          directory: ${{ env.CONFIG_DIRECTORY }}
          speculative: true

      - name: Create Terraform Cloud run (plan only)
        id: plan-run
        uses: hashicorp/tfc-workflows-github/actions/create-run@v1.3.2
        with:
          workspace: ${{ env.TF_WORKSPACE }}
          configuration_version: ${{ steps.plan-upload.outputs.configuration_version_id }}
          plan_only: true

      - name: Fetch plan output
        id: plan-output
        uses: hashicorp/tfc-workflows-github/actions/plan-output@v1.3.2
        with:
          plan: ${{ fromJSON(steps.plan-run.outputs.payload).data.relationships.plan.data.id }}

      - name: Add plan summary to workflow
        shell: bash
        run: |
          {
            echo "## Terraform Cloud Dev Speculative Plan"
            echo ""
            echo "Workspace: \\`${{ env.TF_WORKSPACE }}\\`"
            echo "Run URL: ${{ steps.plan-run.outputs.run_link }}"
            echo ""
            echo "\\`\\`\\`"
            echo "${{ steps.plan-output.outputs.add }}"
            echo "${{ steps.plan-output.outputs.change }}"
            echo "${{ steps.plan-output.outputs.destroy }}"
            echo "\\`\\`\\`"
          } >> "$GITHUB_STEP_SUMMARY"
